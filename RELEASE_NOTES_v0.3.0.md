# v0.3.0 版本说明

## 🚀 重大更新：接入 Ollama 本地大模型

本版本将桌面 Agent 从"纯规则回复"升级为**真正的智能 Agent**，接入本机 Ollama 大模型，支持自然语言对话与自主工具调用。

## 🆕 本次更新亮点

### 🤖 智能大模型
- **接入 Ollama 本地大模型**（默认 `qwen2.5:0.5b`，最小体积、低内存占用）
- 通过 `urllib.request` 调用本机 Ollama `/api/chat` 接口
- **多轮对话记忆**：自动携带最近 `HISTORY_LIMIT=8` 条上下文，对话更连贯
- 模型回复中可输出 `/` 指令，**自动识别并执行本机操作**

### 🏗️ 全新架构
- **新增配置中心** `agent/config.py`：集中管理 Ollama 地址、模型名、采样参数、超时等
- 核心层 `agent/core.py` 重构：对话分发 → 大模型调用 → 工具路由 → 规则兜底
- 工具层 `agent/tools.py`：命令、文件、系统信息操作独立封装

### 🛡️ 健壮性
- **规则兜底**：Ollama 未启动/出错时自动回退到内置规则聊天，程序不崩溃
- 工具执行带超时保护，避免卡死

### 🧑💻 界面优化
- macOS 上 `Label` 模拟发送按钮，解决原生按钮文字不显示问题
- 暗色主题滚动条样式统一

### 📚 文档完善
- README 全面重写：新增**软件架构图**、**数据流转调用链**、**函数接口注释**、配置说明等

## ✨ 工具指令

| 指令 | 参数 | 说明 |
|------|------|------|
| `/ls` | `[路径]` | 列出目录内容 |
| `/cat` | `文件名` | 读取文本文件 |
| `/open` | `[路径]` | 用默认程序打开 |
| `/mkdir` | `路径` | 创建目录 |
| `/shell` | `命令` | 执行 shell 命令（10秒超时） |
| `/sysinfo` | — | 查看系统信息 |
| `/echo` | `文本` | 原样回显 |
| `/help` | — | 查看帮助 |

## 🎨 技术栈
- Python 3.11 + tkinter（标准库）
- **Ollama 本地大模型**（qwen2.5:0.5b）
- psutil（系统信息采集）
- urllib.request（调用大模型接口）

## 📦 安装包
- `DesktopAgent-macos-v0.3.0.zip` —— Release 正式版（隐藏控制台）
- `DesktopAgentDebug-macos-v0.3.0.zip` —— Debug 调试版（保留控制台日志）

## ⚠️ 使用前提
打包后的 App 为独立完整应用，但**大模型能力依赖本机 Ollama 服务**：
```bash
# 安装并启动 Ollama
brew install ollama
ollama serve

# 拉取模型
ollama pull qwen2.5:0.5b
```
若未安装 Ollama，App 自动回退到规则聊天，不影响基本使用。

