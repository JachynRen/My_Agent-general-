# 🚀 DesktopAgent v0.3.0 版本发布说明

> **重大版本更新：从"规则式聊天"升级为"Ollama 大模型驱动的智能 Agent"**

---

## ✨ 版本亮点

本版本将桌面 Agent 从纯规则回复升级为**真正接入大模型的智能助手**：
- 🧠 接入 **Ollama 本地大模型**，支持自然语言对话、多轮记忆、自主调用本机工具
- 🏗️ 引入**分层软件架构**（GUI / 核心 / 工具 / 配置），代码更清晰、可维护
- 🛡️ 内置**规则兜底机制**，Ollama 未启动时程序不崩溃，自动降级为规则聊天
- 📚 **文档全面重写**：新增架构图、数据流转调用链、函数接口注释、配置说明

---

## 🆕 本次更新详解

### 🤖 智能大模型能力
- **接入 Ollama 本地模型**：默认 `qwen2.5:0.5b`（约 0.4GB，低内存友好）
- 通过标准库 `urllib.request` 调用本机 Ollama `/api/chat` 接口，**零第三方依赖**
- **多轮对话记忆**：自动携带最近 `HISTORY_LIMIT=8` 条上下文，连续对话更连贯
- **自主工具调用**：大模型回复中输出 `/` 指令时，Agent 自动识别并执行本机操作
  - 例：问"帮我列一下当前目录" → 模型回复 → 自动执行 `/ls`

### 🏗️ 全新分层架构
```
┌─────────────────────────────────────────────┐
│              GUI 层  agent/ui.py            │  tkinter 聊天窗口
├─────────────────────────────────────────────┤
│             核心层  agent/core.py           │  Agent 类：对话分发/大模型/工具路由
├─────────────────────────────────────────────┤
│   工具层  agent/tools.py    配置层  config.py│  本机操作 / Ollama 参数
└─────────────────────────────────────────────┘
```

### 🛡️ 健壮性设计
- **规则兜底**：Ollama 调用失败/超时 → 自动回退到内置规则聊天（问候、报时间等）
- **超时保护**：大模型调用 60 秒超时，工具命令 10 秒超时，避免卡死
- **记忆裁剪**：自动控制对话历史长度，防止上下文无限膨胀

### 🧑‍💻 界面优化
- macOS 上改用 `Label` 模拟发送按钮，解决原生按钮文字不显示问题
- 暗色主题滚动条样式统一（灰色滑块 + 暗色轨道）

### 📚 文档完善
- README 全面重写：架构图、数据流转调用链、函数接口注释、配置说明等

---

## 🛠️ 工具指令

| 指令 | 参数 | 说明 |
|------|------|------|
| `/ls` | `[路径]` | 列出目录内容 |
| `/cat` | `文件名` | 读取文本文件（限制 200KB） |
| `/open` | `[路径]` | 用系统默认程序打开 |
| `/mkdir` | `路径` | 创建目录（含父目录） |
| `/shell` | `命令` | 执行 shell 命令（10 秒超时） |
| `/sysinfo` | — | 查看系统信息（CPU/内存/磁盘/开机时间） |
| `/echo` | `文本` | 原样回显 |
| `/help` | — | 显示帮助 |

---

## 🎨 技术栈

| 组件 | 说明 |
|------|------|
| Python | 3.11 |
| GUI | tkinter（标准库） |
| 大模型 | Ollama `qwen2.5:0.5b` |
| API 调用 | `urllib.request`（标准库） |
| 系统信息 | psutil |
| 打包 | PyInstaller |

---

## 📦 安装包

| 文件 | 说明 |
|------|------|
| `DesktopAgent-macos-v0.3.0.zip` | **Release 正式版**：隐藏控制台，适合日常使用 |
| `DesktopAgentDebug-macos-v0.3.0.zip` | **Debug 调试版**：保留控制台窗口，便于查看日志 |

---

## ⚠️ 使用前提

打包后的 App 是**独立完整应用**（已内置 Python/tkinter/psutil），但**大模型能力依赖本机 Ollama 服务**：

```bash
# 1. 安装 Ollama（macOS）
brew install ollama

# 2. 启动 Ollama 服务
ollama serve

# 3. 拉取模型（约 0.4GB）
ollama pull qwen2.5:0.5b
```

> 💡 若未安装/启动 Ollama，App **不会崩溃**，会自动回退到内置规则聊天（能问候、报时间等基础功能）。

### macOS 首次打开提示
未做 Apple 公证，Gatekeeper 可能提示"无法验证开发者"。解决方法：
- **右键**点击 `.app` → **打开**；或
- 终端执行：`xattr -d com.apple.quarantine /路径/DesktopAgent.app`

---

## 📈 版本历史

| 版本 | 说明 |
|------|------|
| **v0.3.0** | 🎉 接入 Ollama 大模型，分层架构，规则兜底（本次） |
| v0.2.0 | UI 优化（Label 按钮、暗色滚动条） |
| v0.1.0 | 规则式桌面 Agent Demo |

---

感谢使用！如有问题，请在 GitHub Issues 反馈。🙏
